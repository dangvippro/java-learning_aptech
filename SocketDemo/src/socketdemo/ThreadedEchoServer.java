package socketdemo;

import java.net.Socket;

public class ThreadedEchoServer {
    public static int defaultPort = 2000;
    Socket theConnnection;
    
    //Constractor
    public ThreadedEchoServer(Socket s) {
        theConnnection = s;
    }
    
    public static void main(String[] args) {
        Server 
    }
}
